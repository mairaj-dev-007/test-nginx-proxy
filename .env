POSTGRES_USER=litellm_user
POSTGRES_PASSWORD=litellm_password
POSTGRES_DB=litellm_db
LITELLM_MASTER_KEY=AzELwnD2jksLB630OHdyllibYNcufTzhT4cQvUHFlMQ=
OPENAI_API_KEY=your-openai-api-key-here

# Nginx-style proxy configuration

# Define upstream servers (backend servers)
UPSTREAM_SERVERS={"litellm": "http://litellm:4000"}

# Default upstream to use when no server config matches
DEFAULT_UPSTREAM=litellm

# Server configuration (routes domains to upstreams with IP whitelisting)
# Format: {"domain": {"upstream": "server_name", "allowed_ips": ["ip1", "ip2"]}}
# Examples:
# Single domain: {"localhost": {"upstream": "litellm", "allowed_ips": ["127.0.0.1", "::1"]}}
# Multiple domains: {"api.example.com": {"upstream": "litellm", "allowed_ips": ["203.0.113.1"]}, "localhost": {"upstream": "litellm"}}
# Leave as {} to allow all domains to default upstream
SERVER_CONFIG={}
